# AGENTS.md

**Your operating schema lives in [`CLAUDE.md`](CLAUDE.md). Read it now before doing anything else.**

This file exists only to redirect agents whose host tool auto-loads `AGENTS.md` (Cursor, Codex, and other AGENTS.md-compatible tools). The actual schema — directory layout, page format, workflows (ingest / query / lint / instill), scheduler usage, operating principles — is all in `CLAUDE.md`.

Treat `CLAUDE.md` as if it were named `AGENTS.md`. There is no separate `AGENTS.md` schema.
